function CheckEmailExist() {    var email = $("#Email").val();     $.ajax({        url: '/PatientReq/CheckEmailExist',         type: 'POST',        dataType: 'json',        data: { email: email },        success: function (response) {            if (response.exists) {                              $('#emailError').text("Email already exists");                $('#Email').focus();                 $('#password').hide();                $('#confirmPassword').hide();            }            else {                $('#emailError').text("");                $('#password').show();                $('#confirmPassword').show();            }        }    });}